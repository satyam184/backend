# backend

Simple Node.js authentication backend built with Express and MongoDB.

## Run locally

1. Install dependencies with `npm install`
2. Create a `.env` file with `PORT`, `MONGO_URI`, and `JWT_SECRET`
3. Start the server with `npm run dev`
